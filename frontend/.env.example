# Nifty Future Analyzer – Frontend Environment Variables
# Copy to .env and adjust

# Backend API URL (leave as /api when using Vite proxy)
VITE_API_URL=/api

# Or point directly to the backend for production:
# VITE_API_URL=http://your-server:8000/api
